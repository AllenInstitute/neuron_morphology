Parameters:

  Prefix:
    Type: String
    Description: Used to prefix resources (we can't use stack name b/c the autogenerated names may not be valid for all resources)

Resources:

  OutputDataBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "${Prefix}-data"
